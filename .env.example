# Frontend environment variables (Vite)
# Copy to .env and set your API URL

# Local dev (Nest API default)
VITE_API_URL=http://localhost:3000

# Example for Render (when deployed)
# VITE_API_URL=https://<your-api-on-render>.onrender.com
